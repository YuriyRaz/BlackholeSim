# Request

TDE mode hang the Chrome browser. Use default dev cycle to findout the reason and fix this.

## Context

The project is a Blackhole Simulator. TDE (Tidal Disruption Event) mode causes Chrome to hang. The user wants investigation and a fix using the default development cycle.
