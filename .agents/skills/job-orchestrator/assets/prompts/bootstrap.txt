Worker subagent bootstrap payload. The root orchestrator must send this text to the job subagent; the root must not execute it.
Bootstrap only. Do not perform domain work.
Read frozen protocol: {{protocol}}
Read contract: {{contract}}
Recovery checkpoint (if non-empty): {{checkpoint}}
Completed work units: {{completed}}
Exact next permitted action: {{next_permitted}}
Validate both and emit the canonical acknowledgement with:
python {{workerctl}} acknowledge --contract "{{contract}}" --protocol-version {{protocol_version}} --protocol-sha256 {{protocol_sha256}} --job-id "{{job_id}}" --contract-revision {{contract_revision}} --current-node "{{node}}" --session-id "<transport-session-id>"
Use the exact session ID assigned by the transport in place of <transport-session-id>. Return only the JSON emitted by this command.
