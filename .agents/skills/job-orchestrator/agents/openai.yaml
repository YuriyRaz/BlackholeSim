interface:
  display_name: "Job Orchestrator"
  short_description: "Run durable, bounded, recoverable job workflows"
  default_prompt: "Use $job-orchestrator to execute this request through its script-enforced bounded job queue."
